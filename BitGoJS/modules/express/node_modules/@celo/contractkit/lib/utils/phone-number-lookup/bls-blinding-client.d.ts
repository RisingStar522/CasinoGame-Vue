export interface BlsBlindingClient {
    blindMessage: (base64PhoneNumber: string) => Promise<string>;
    unblindAndVerifyMessage: (blindedMessage: string) => Promise<string>;
}
export declare class WasmBlsBlindingClient implements BlsBlindingClient {
    private thresholdBls;
    private blindedValue;
    private pgpnpPubKey;
    private rawMessage;
    constructor(pgpnpPubKey: string);
    blindMessage(base64PhoneNumber: string): Promise<string>;
    unblindAndVerifyMessage(base64BlindSig: string): Promise<string>;
    private isReactNativeEnvironment;
}
